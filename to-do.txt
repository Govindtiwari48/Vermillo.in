1: add the marquee continuously moving line and add this conetent,
{/* Professional Marquee Bar */}

<AnimatePresence>

  {logoAnimationComplete && (

    <motion.div

      initial={{ opacity: 0, y: 20 }}

      animate={{ opacity: 1, y: 0 }}

      exit={{ opacity: 0, y: 20 }}

      transition={{ duration: 0.6, delay: 1 }}

      className="absolute bottom-0 left-0 right-0 z-40 bg-black/90 backdrop-blur-sm border-t border-white/10"

    >

      <div className="overflow-hidden py-4">

        <div className="flex whitespace-nowrap animate-marquee flex-nowrap">

          {/* Multiple iterations for seamless scroll */}

          {Array(4).fill(0).map((_, i) => (

            <div key={i} className="flex items-center gap-8 px-4">

              <span className="text-white text-sm md:text-base font-semibold">

                <span style={{ color: '#FF6600' }}>MADE IN INDIA </span>

                <span className="text-white ml-2">FOR THE WORLD</span>

              </span>

              <span className="text-white text-sm md:text-base font-semibold">

                <span style={{ color: '#FF6600' }}>FLAT 10%</span>

                <span className="text-white ml-2">OFF ON FIRST PURCHASE</span>

              </span>

              <span className="text-white/60 text-xs md:text-sm">//</span>

              <span className="text-white font-medium text-sm md:text-base">

                FREE 7 DAY RETURNS

              </span>

              <span className="w-2 h-2 bg-white/40 rounded-full"></span>

            </div>

          ))}

        </div>

      </div>

    </motion.div>

  )}

</AnimatePresence>



tailwind config

animation: {

  marquee: 'marquee 30s linear infinite',

},

keyframes: {

  marquee: {

    '0%': { transform: 'translateX(0)' },

    '100%': { transform: 'translateX(-50%)' },

  },

},



css animation

/* Marquee Animation */

@keyframes marquee {

  0% {

    transform: translateX(0);

  }



  100% {

    transform: translateX(-50%);

  }

}

2: add this prompt
 remove the bottom width of the hero section and add the marquee line
 Add the marquee link propefessional darked theme written text "add though your own intelligece, "MADE IN INDIA [IN ORANGE COLOR] FOR THE WORLD [IN WHITE COLOR], // 
[1-2=DONE]


3: remove the curve grid edges and make it pointy [Done]

4: from SHOP BY CATEGORY, below 
remove all the tag line and other button and text from the images, 
also fix the "10K+" make it equal in font size

5: hero section mlabel shi krna h  [Done]

6: Added teh manual scrol control option so that i can see the product of my own [Done]

7: Added the wishlist feature [Done]

8: add the ss taken images from the pushpraj instagram [Done]

9: remove eye image [Done]
10: PAinting section m sketches bhi show krado, and make sure inside that pafe we can show the category of both sketch and painting [Done]
id, 4,6,9,11

11: Fix the font of each of the page and make it professional
12: Align to mid
13: add Add to cart and Buy Now option [done]


save use details, so that they dont need to enter again and again, address and payment way




// Import the functions you need from the SDKs you need //firebase SDK to connect with firebase
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyAKiFq-TmGEw5iy7QsdDbzi57KJ8ZVDJQg",
  authDomain: "vermillo.firebaseapp.com",
  projectId: "vermillo",
  storageBucket: "vermillo.firebasestorage.app",
  messagingSenderId: "544137271576",
  appId: "1:544137271576:web:482bf9c6a06ef1c3d1e66d",
  measurementId: "G-3QP72DNWTF"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);